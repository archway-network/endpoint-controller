apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "endpoint-controller.fullname" . }}-test-connection"
  labels:
    {{- include "endpoint-controller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "endpoint-controller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
